export enum UserType {
    ADMIN = 'ADMIN',
    USER = 'USER',
    GUEST = 'GUEST',
    STAFF = 'STAFF',
    
}

export enum UserStatus {
    PENDING = 'Pending',
    ACTIVE = 'Active',
    BLOCKED = 'Blocked',
  }
